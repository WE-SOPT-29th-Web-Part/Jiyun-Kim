export const qs = (selector) => document.querySelector(selector);
export const qsAll = (selector) => document.querySelectorAll(selector);
export const createElem = (element) => document.createElement(element);